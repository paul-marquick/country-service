web service needs

    health / liveness endpoint, with db conn check.

    metrics endpoint.

    config endpoint - see all web service config.

    options endpoint.
 

angular needs

    display server errors after submit

    get spinner working

    add country component

    delete country component

    Create grid component.

    csrf 

Create a blazor app to match angular app.
